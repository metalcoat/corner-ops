MILESTONE: Barcode and Merchandise Scanning
RESULT: PASS
SUMMARY: Implemented business-scoped, stable item/variant barcode mappings with zero-to-many mappings per target, active/inactive history, database-enforced duplicate active barcode prevention, explicit UPC-A/EAN-8/EAN-13/GTIN-14/Code-128-text format patterns, manager/owner management, authoritative server-side employee denial, and complete mapping audit snapshots. Added USB/Bluetooth HID-compatible rapid keyboard-wedge detection that ignores ordinary human text entry. Menu scans resolve only merchandise, retain availability and backend pricing authority, quick-add simple products, and open configuration for required variants/modifiers/combos. Unknown scans produce a clear message with manager-only mapping. Checkout scans route to the existing gift-card tender UI for review and explicit application; Menu scans never invoke gift-card operations. Added mapping management settings and import-safe non-cascading stable catalog references.
TESTS: PASS — npm run typecheck; npm run build; npm run test:barcodes; npm run test:gift-cards; npm run test:payments; npm run test:ordering:lifecycle; npm run test:menu-refinement; full POS Playwright suite 11/11 with generated temporary fixture employees/PINs followed by cleanup. Barcode validation covers formats, rapid-vs-human input, stable item/variant resolution, multiple mappings, duplicate prevention, inactive lookup exclusion, employee authorization denial, and audit entries. Application health is OK; PostgreSQL is healthy; port 5432 remains unpublished. Initial POS runs were environment-only failures (missing Chromium, then missing fixture variables); the browser was installed under /tmp and the properly provisioned rerun passed.
BLOCKERS: None.
NEXT RECOMMENDATION: Have the outer controller review and commit this isolated milestone, then deploy only through its authorized development-branch workflow. Validate physical scanner suffix/timing settings on target register hardware before rollout.

AUTOBUILD_RESULT: PASS
