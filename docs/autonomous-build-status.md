MILESTONE: Payment Operations Hardening
RESULT: PASS
SUMMARY: Added manager/owner-authorized tender void and reversal operations with required reasons, integer-cent partial reversals, correct order and split-check balance restoration, immutable linked reversal transactions, request idempotency, duplicate/over-reversal prevention, and gift-card balance restoration through an immutable related ledger entry. Added authenticated, reasoned payment receipt reprints that create only reprint print jobs and never duplicate payment or kitchen production. Added POS controls for receipt reprint and manager reversal, retained mixed tenders and split checks, and audited sensitive reversal and reprint actions in order events. Existing post-send PAYMENT UPDATE slips retain paid-this-update, total-paid, remaining-due, and final PAID/zero-due payloads; failed transactions remain atomic and create no slip.
TESTS: PASS — npm run typecheck; npm run build; npm run test:payments; npm run test:gift-cards; npm run test:split-checks; npm run test:order-void; npm run test:ordering:lifecycle; full POS Playwright suite 11/11 with ephemeral undisclosed fixture PINs followed by fixture cleanup. Payment validation covers manager authorization, partial reversal balances, immutable reversal ledger, reversal idempotency, partial/final PAYMENT UPDATE payloads, audited receipt reprint, mixed tenders, and no kitchen-ticket duplication. Initial Playwright attempts were environment-only failures due to missing fixture PIN configuration and then missing Chromium; the final isolated /tmp Chromium run passed. Application health is OK; PostgreSQL is healthy; port 5432 remains unpublished.
BLOCKERS: None.
NEXT RECOMMENDATION: Have the outer controller review and commit this isolated milestone, then deploy only through its authorized development-branch workflow. Exercise tender reversal and receipt reprint against configured development printers before register rollout.

AUTOBUILD_RESULT: PASS
