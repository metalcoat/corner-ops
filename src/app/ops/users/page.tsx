"use client";

import { FormEvent, useEffect, useState } from "react";
import type { Business, SessionView } from "@/lib/types";
import "../control-center.css";

type User={id:string;email:string;displayName:string;role:string;businesses:Business[];permissions:string[];active:boolean;createdBy:string;createdAt:string;legacyOwner:boolean};
async function msg(r:Response){const p=await r.json().catch(()=>null) as {error?:string}|null;return p?.error||`Request failed (${r.status}).`;}
export default function UsersPage(){const [session,setSession]=useState<SessionView|null>(null),[users,setUsers]=useState<User[]>([]),[notice,setNotice]=useState(""),[busy,setBusy]=useState(false),[editing,setEditing]=useState<User|null>(null);useEffect(()=>{fetch("/api/auth/session",{cache:"no-store"}).then(r=>r.json()).then(setSession)},[]);async function load(){const r=await fetch("/api/users",{cache:"no-store"});if(!r.ok)throw new Error(await msg(r));setUsers((await r.json()).users)}useEffect(()=>{if(session?.authenticated)load().catch(e=>setNotice(e.message))},[session?.authenticated]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);try{const f=new FormData(e.currentTarget);const r=await fetch("/api/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:editing?.id,email:f.get("email"),displayName:f.get("displayName"),role:f.get("role"),businesses:[f.get("cornerDeli")==="on"?"Corner Deli":null,f.get("tiki")==="on"?"Tiki":null].filter(Boolean),password:f.get("password")||undefined,active:true})});if(!r.ok)throw new Error(await msg(r));e.currentTarget.reset();setEditing(null);await load();setNotice(editing?"User updated.":"User created.")}catch(e){setNotice(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}async function active(user:User){setBusy(true);try{const r=await fetch("/api/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"active",id:user.id,active:!user.active})});if(!r.ok)throw new Error(await msg(r));await load();setNotice(`${user.displayName} is now ${user.active?"inactive":"active"}.`)}catch(e){setNotice(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}if(!session)return <main className="controlPage">Loading…</main>;if(!session.authenticated)return <main className="controlPage"><a href="/signin">Sign in</a></main>;return <main className="controlPage"><header className="controlHeader"><div><p className="eyebrow">Access control</p><h1>Corner Ops users</h1><p>Separate accounts, scoped businesses, and roles. One shared password has finally been relieved of governing an empire.</p></div><div className="controlActions"><a href="/ops">Operations</a><a href="/signin">User sign-in</a></div></header>{notice&&<div className="noticeBar">{notice}</div>}<div className="controlGrid"><section className="controlCard half"><p className="eyebrow">{editing?"Edit account":"New account"}</p><h2>{editing?.displayName||"Add user"}</h2><form className="controlForm" onSubmit={save} key={editing?.id||"new"}><label>Name<input name="displayName" defaultValue={editing?.displayName||""} required/></label><label>Email<input name="email" type="email" defaultValue={editing?.email||""} required/></label><label>Role<select name="role" defaultValue={editing?.role||"Viewer"}><option>Co-Owner</option><option>Accountant</option><option>Manager</option><option>Viewer</option>{editing?.role==="Owner"&&<option>Owner</option>}</select></label><label>{editing?"New password, optional":"Temporary password"}<input name="password" type="password" minLength={10} required={!editing}/></label><label><span><input name="cornerDeli" type="checkbox" defaultChecked={editing?editing.businesses.includes("Corner Deli"):true}/> Corner Deli</span></label><label><span><input name="tiki" type="checkbox" defaultChecked={editing?editing.businesses.includes("Tiki"):true}/> Tiki</span></label><div className="controlActions wide"><button className="primary" disabled={busy}>{editing?"Save user":"Create user"}</button>{editing&&<button type="button" onClick={()=>setEditing(null)}>Cancel</button>}</div></form></section><section className="controlCard half"><p className="eyebrow">Role map</p><h2>Permissions</h2><div className="list"><div className="listItem"><strong>Owner / Co-Owner</strong><span>Everything</span></div><div className="listItem"><strong>Accountant</strong><span>Accounting, integrations, payroll and reports</span></div><div className="listItem"><strong>Manager</strong><span>Payroll, workforce, documents and reports</span></div><div className="listItem"><strong>Viewer</strong><span>Read-only operating visibility</span></div></div></section><section className="controlCard"><p className="eyebrow">Active directory</p><h2>Users</h2><div className="tableWrap"><table className="controlTable"><thead><tr><th>User</th><th>Role</th><th>Businesses</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td><strong>{user.displayName}</strong><small>{user.email}</small></td><td>{user.role}</td><td>{user.businesses.join(" · ")}</td><td><span className={`badge ${user.active?"good":"bad"}`}>{user.active?"Active":"Inactive"}</span></td><td>{new Date(user.createdAt).toLocaleDateString()}<small>{user.createdBy}</small></td><td><button onClick={()=>setEditing(user)}>Edit</button> <button onClick={()=>active(user)} disabled={busy||user.role==="Owner"}>{user.active?"Deactivate":"Reactivate"}</button></td></tr>)}</tbody></table></div></section></div></main>}
