"use client";

import { FormEvent, useState } from "react";
import "../ops/control-center.css";

export default function SignInPage(){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const f=new FormData(e.currentTarget);try{const r=await fetch("/api/auth/session",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});const p=await r.json().catch(()=>({})) as {error?:string};if(!r.ok)throw new Error(p.error||"Sign-in failed.");window.location.href="/ops"}catch(e){setMessage(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}return <main className="controlPage" style={{display:"grid",placeItems:"center"}}><section className="controlCard" style={{maxWidth:520,width:"100%"}}><p className="eyebrow">Corner Ops</p><h1>Account sign-in</h1><p className="muted">Use the email and password assigned to your role.</p><form className="controlForm" onSubmit={submit}><label className="wide">Email<input name="email" type="email" autoComplete="username" required autoFocus/></label><label className="wide">Password<input name="password" type="password" autoComplete="current-password" required/></label>{message&&<div className="noticeBar wide">{message}</div>}<button className="primary" disabled={busy}>{busy?"Signing in…":"Sign in"}</button><a href="/forgot-password">Forgot password?</a><a href="/employee">Employee Hub</a></form></section></main>}
