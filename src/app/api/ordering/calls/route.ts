import { orderingActor } from "@/lib/ordering-route-auth";
import { acknowledgeDeliCall,activeDeliCalls } from "@/lib/three-cx-live-calls";
import { ensureOrderingAiSchema } from "@/lib/ordering-ai-schema";
import { getSql } from "@/lib/db";
export const runtime="nodejs";
async function activeAiCalls(){await ensureOrderingAiSchema();return getSql()`SELECT call.id,call.three_cx_call_id call_id,call.caller_phone,call.called_did,call.line_label line_number,call.state,call.handoff_reason,call.started_at,call.updated_at,call.order_id open_order_id,call.claimed_by,call.claimed_at,customer.id customer_id,customer.display_name,orders.display_number open_order_number,orders.status open_order_status,orders.service_type,orders.subtotal_cents,orders.tax_cents,orders.total_cents,COALESCE((SELECT jsonb_agg(jsonb_build_object('id',item.id,'name',item.item_name_snapshot,'quantity',item.quantity,'lineTotalCents',item.line_total_cents,'instructions',item.special_instructions,'modifiers',COALESCE((SELECT jsonb_agg(jsonb_build_object('name',modifier.option_name_snapshot,'quantity',modifier.quantity) ORDER BY modifier.created_at) FROM ordering_order_item_modifiers modifier WHERE modifier.order_item_id=item.id),'[]'::jsonb)) ORDER BY item.sort_order,item.created_at) FROM ordering_order_items item WHERE item.order_id=orders.id),'[]'::jsonb) order_items,COALESCE((SELECT jsonb_agg(jsonb_build_object('id',activity.id,'createdAt',activity.created_at,'role',activity.role,'label',activity.label,'detail',activity.detail,'durationMs',activity.duration_ms) ORDER BY activity.created_at) FROM (SELECT event.id::text id,event.created_at,event.role,event.label,event.detail,event.duration_ms FROM ordering_ai_call_events event WHERE event.business='Corner Deli' AND event.call_id=call.three_cx_call_id UNION ALL SELECT tool.id::text,tool.created_at,'tool',CASE WHEN tool.outcome='success' THEN 'Completed '||replace(tool.tool_name,'_',' ') ELSE 'Problem with '||replace(tool.tool_name,'_',' ') END,CASE WHEN tool.outcome='success' THEN '' ELSE tool.error_code END,tool.duration_ms FROM ordering_ai_tool_events tool WHERE tool.business='Corner Deli' AND tool.conversation_id=call.three_cx_call_id ORDER BY created_at DESC LIMIT 30)activity),'[]'::jsonb) activities FROM ordering_call_sessions call LEFT JOIN ordering_orders orders ON orders.id=call.order_id LEFT JOIN ordering_customers customer ON customer.id=COALESCE(call.customer_id,orders.customer_id) WHERE call.business='Corner Deli' AND call.state IN('ai','handoff_pending','human') AND call.updated_at>NOW()-INTERVAL '4 hours' ORDER BY CASE call.state WHEN 'handoff_pending' THEN 0 WHEN 'human' THEN 1 ELSE 2 END,call.started_at`}
async function aiOrderItems(orderId:string){if(!orderId)return[];return getSql()`SELECT item.id,item.item_name_snapshot name,item.variant_name_snapshot variant,item.quantity,item.line_total_cents "lineTotalCents",item.special_instructions instructions,COALESCE((SELECT jsonb_agg(jsonb_build_object('name',modifier.option_name_snapshot,'quantity',modifier.quantity)ORDER BY modifier.created_at)FROM ordering_order_item_modifiers modifier WHERE modifier.order_item_id=item.id AND modifier.print_on_ticket=TRUE),'[]'::jsonb) modifiers FROM ordering_order_items item WHERE item.order_id=${orderId} ORDER BY item.sort_order,item.created_at`}
export async function GET(){const actor=await orderingActor("Corner Deli");if(!actor){console.warn("[ordering-calls] poll rejected: POS authentication required");return Response.json({error:"POS authentication required."},{status:401})}const [calls,aiCalls]=await Promise.all([activeDeliCalls(),activeAiCalls()]),enrichedAiCalls=await Promise.all(aiCalls.map(async call=>({...call,order_items:await aiOrderItems(String(call.open_order_id||""))})));if(calls.length)console.info("[ordering-calls] active caller delivered",{employeeId:actor.id,count:calls.length,callId:String(calls[0].call_id)});return Response.json({calls,aiCalls:enrichedAiCalls,employeeId:actor.id},{headers:{"Cache-Control":"no-store"}})}
export async function PATCH(request:Request){const actor=await orderingActor("Corner Deli");if(!actor)return Response.json({error:"POS authentication required."},{status:401});const body=await request.json(),action=String(body.action||"acknowledge"),id=String(body.id||"");if(action==="acknowledge")return Response.json({ok:await acknowledgeDeliCall(id,actor.id)});await ensureOrderingAiSchema();if(action==="clear_test_calls"){const rows=await getSql()`UPDATE ordering_call_sessions SET state='ended',ended_at=COALESCE(ended_at,NOW()),claimed_by='',claimed_at=NULL,owner_type='none',owner_id='',updated_at=NOW() WHERE business='Corner Deli' AND state IN('ai','handoff_pending','human') RETURNING id`;return Response.json({ok:true,cleared:rows.length})}if(action==="claim"){const rows=await getSql()`UPDATE ordering_call_sessions SET claimed_by=${actor.id},claimed_at=NOW(),owner_type='employee',owner_id=${actor.id},state='human',updated_at=NOW() WHERE id=${id} AND business='Corner Deli' AND state IN('handoff_pending','human') AND (claimed_by='' OR claimed_by=${actor.id}) RETURNING id,claimed_by,state`;return rows[0]?Response.json({ok:true,call:rows[0]}):Response.json({error:"This intervention was claimed on another POS."},{status:409})}if(action==="release"){const rows=await getSql()`UPDATE ordering_call_sessions SET claimed_by='',claimed_at=NULL,owner_type='none',owner_id='',state='handoff_pending',updated_at=NOW() WHERE id=${id} AND business='Corner Deli' AND claimed_by=${actor.id} RETURNING id`;return Response.json({ok:Boolean(rows[0])})}if(action==="complete"){const rows=await getSql()`UPDATE ordering_call_sessions SET state='ended',ended_at=NOW(),updated_at=NOW() WHERE id=${id} AND business='Corner Deli' AND (claimed_by=${actor.id} OR claimed_by='') RETURNING id`;return Response.json({ok:Boolean(rows[0])})}return Response.json({error:"Unknown call action."},{status:400})}
