import{getSql}from"@/lib/db";import{get}from"@/lib/storage";import{ensureOrderingMenuOverrideSchema}from"@/lib/ordering-menu-overrides";
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){await ensureOrderingMenuOverrideSchema();const{id}=await params,rows=await getSql()`SELECT storage_reference,mime_type FROM ordering_menu_media WHERE id=${id}`;if(!rows[0])return new Response("Not found",{status:404});const object=await get(String(rows[0].storage_reference),{access:"public",ifNoneMatch:req.headers.get("if-none-match")||undefined});if(!object)return new Response("Not found",{status:404});return new Response(object.stream,{status:object.statusCode,headers:{"content-type":String(rows[0].mime_type),etag:object.blob.etag,"cache-control":"public,max-age=3600"}})}
