import { driverActor, recordLocation } from "@/lib/ordering-driver-delivery";
export const runtime="nodejs";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await driverActor();if(!actor)return Response.json({error:"Employee sign-in required."},{status:401});const body=await request.json() as Record<string,unknown>;return Response.json(await recordLocation(actor,(await params).id,{clientEventId:String(body.clientEventId||""),capturedAt:String(body.capturedAt||""),latitude:Number(body.latitude),longitude:Number(body.longitude),accuracy:Number(body.accuracy),metadata:body.metadata}))}catch(error){const message=error instanceof Error?error.message:"Location was not accepted.";return Response.json({error:message},{status:message.includes("assigned")?403:409})}}
