"use client";
import {useEffect,useState} from "react";
type Item={id:string;item_name_snapshot:string;quantity:number;cancelled_quantity?:number};
type Tender={id:string;tender_type:string;transaction_type:string;status:string;amount_cents:number;related_transaction_id?:string|null};
type Quote={orderReductionCents:number;refundRequiredCents:number;discountCents:number;taxCents:number};
const money=(c:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(c/100);
export default function ItemCancellationPanel({orderId,item,onDone,onClose}:{orderId:string;item:Item;onDone:()=>Promise<void>;onClose:()=>void}){const available=Number(item.quantity)-Number(item.cancelled_quantity||0),[quantity,setQuantity]=useState(1),[reason,setReason]=useState(""),[quote,setQuote]=useState<Quote|null>(null),[tenders,setTenders]=useState<Tender[]>([]),[tenderId,setTenderId]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{setQuote(null);const timer=setTimeout(async()=>{try{const [q,p]=await Promise.all([fetch(`/api/ordering/order-center/${orderId}/items/cancel`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"quote",itemId:item.id,quantity})}),fetch(`/api/ordering/orders/${orderId}/payments?business=Corner%20Deli`)]),qb=await q.json(),pb=await p.json();if(!q.ok)throw new Error(qb.error||"Cancellation could not be priced.");setQuote(qb);const payments=(pb.tenders||[]).filter((row:Tender)=>row.transaction_type==="payment"&&row.status==="approved");setTenders(payments);setTenderId(current=>current||payments[0]?.id||"");setError("")}catch(e){setError(e instanceof Error?e.message:"Cancellation could not be priced.")}},150);return()=>clearTimeout(timer)},[item.id,orderId,quantity]);async function submit(e:React.FormEvent){e.preventDefault();if(!quote)return;setBusy(true);try{const response=await fetch(`/api/ordering/order-center/${orderId}/items/cancel`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({itemId:item.id,quantity,reason,paymentTransactionId:quote.refundRequiredCents?tenderId:undefined,clientMutationId:crypto.randomUUID()})}),body=await response.json();if(!response.ok)throw new Error(body.error||"Item cancellation failed.");await onDone()}catch(e){setError(e instanceof Error?e.message:"Item cancellation failed.")}finally{setBusy(false)}}return <form className="ocItemCancel" onSubmit={submit}><h3>Cancel item</h3><strong>{item.item_name_snapshot}</strong><label>Quantity<select value={quantity} onChange={e=>setQuantity(Number(e.target.value))}>{Array.from({length:available},(_,i)=><option key={i+1}>{i+1}</option>)}</select></label><label>Reason<textarea autoFocus maxLength={500} value={reason} onChange={e=>setReason(e.target.value)}/></label>{quote&&<div className="cancelQuote"><span>Order reduction <b>{money(quote.orderReductionCents)}</b></span><span>Includes tax adjustment <b>{money(quote.taxCents)}</b></span><span>Refund required <b>{money(quote.refundRequiredCents)}</b></span></div>}{quote&&quote.refundRequiredCents>0&&<label>Reverse from tender<select value={tenderId} onChange={e=>setTenderId(e.target.value)}><option value="">Choose tender</option>{tenders.map(t=><option key={t.id} value={t.id}>{t.tender_type.replaceAll("_"," ")} · {money(t.amount_cents)}</option>)}</select></label>}{error&&<p className="ocDialogError" role="alert">{error}</p>}<div><button type="button" disabled={busy} onClick={onClose}>BACK</button><button className="danger" disabled={busy||reason.trim().length<3||!quote||(quote.refundRequiredCents>0&&!tenderId)}>{busy?"SAVING…":quote?.refundRequiredCents?"CANCEL & REFUND":"CANCEL ITEM"}</button></div></form>}
