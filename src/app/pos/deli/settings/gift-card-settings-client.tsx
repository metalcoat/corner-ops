"use client";
import { useState } from "react";
const cents=(value:string)=>Math.round(Number(value)*100),key=()=>crypto.randomUUID(),money=(value:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(value/100);
export default function GiftCardSettingsClient(){const[cardNumber,setCardNumber]=useState(''),[pin,setPin]=useState(''),[amount,setAmount]=useState(''),[reason,setReason]=useState(''),[result,setResult]=useState<any>(null),[message,setMessage]=useState(''),[report,setReport]=useState<any>(null),[importText,setImportText]=useState('');async function call(method:string,body?:Record<string,unknown>,query=''){setMessage('');const response=await fetch(`/api/ordering/gift-cards${query}`,{method,headers:{'content-type':'application/json'},body:body?JSON.stringify({business:'Corner Deli',...body}):undefined}),data=await response.json();if(!response.ok){setMessage(data.error||'Operation failed.');return null}setResult(data);return data}async function lookup(){await call('GET',undefined,`?business=Corner%20Deli&cardNumber=${encodeURIComponent(cardNumber)}&pin=${encodeURIComponent(pin)}`)}async function mutate(action:string){const data=await call('PATCH',{action,cardNumber,pin,amountCents:cents(amount),deltaCents:cents(amount),reason,operationKey:key()});if(data)setMessage(`${action} completed.`)}async function loadReport(){const response=await fetch('/api/ordering/gift-cards/report?business=Corner%20Deli'),data=await response.json();if(response.ok)setReport(data);else setMessage(data.error)}async function previewImport(){const rows=importText.split('\n').filter(Boolean).map(line=>{const[sourceReference,number,balance]=line.split(',');return{sourceReference,cardNumber:number,balanceCents:cents(balance)}});const response=await fetch('/api/ordering/gift-cards/import',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({business:'Corner Deli',batchKey:'ui-preview',dryRun:true,rows})}),data=await response.json();setResult(data);setMessage(response.ok?'Dry-run complete. No cards were imported.':data.error)}return <section className="giftCardSettings"><h2>Gift cards</h2><p>Activate, load, inspect, and manage durable gift-card accounts. Full numbers and PINs are never displayed after entry.</p><div><label>Card number<input value={cardNumber} onChange={e=>setCardNumber(e.target.value)} autoComplete="off"/></label><label>Optional PIN<input value={pin} onChange={e=>setPin(e.target.value)} type="password" inputMode="numeric" autoComplete="off"/></label><label>Amount / adjustment (+ or −)<input value={amount} onChange={e=>setAmount(e.target.value)} inputMode="decimal"/></label><label>Manager reason<input value={reason} onChange={e=>setReason(e.target.value)}/></label></div><button onClick={()=>void lookup()}>LOOK UP</button><button onClick={async()=>{const data=await call('POST',{initialLoadCents:cents(amount),pin,operationKey:key()});if(data){setCardNumber(data.cardNumber||'');setMessage('Card activated. Record the number now; it will only be masked later.')}}}>ACTIVATE + INITIAL LOAD</button><button onClick={()=>void mutate('reload')}>RELOAD</button><button onClick={()=>void mutate('adjust')}>MANAGER ADJUST</button><button onClick={()=>void mutate('replace')}>REPLACE / DEACTIVATE</button>{result?.card&&<article><strong>{result.card.masked_number}</strong><span>{money(Number(result.card.current_balance_cents))} · {result.card.status}</span>{result.history?.map((row:any)=><small key={row.id}>{row.entry_type} · {money(Number(row.delta_balance_cents))} · {new Date(row.created_at).toLocaleString()}</small>)}</article>}{result?.cardNumber&&<p role="status">New card: <strong>{result.cardNumber}</strong> · {money(Number(result.balanceCents))}</p>}<h3>Outstanding balance report</h3><button onClick={()=>void loadReport()}>REFRESH REPORT</button>{report&&<p>{money(Number(report.summary.outstanding_balance_cents))} outstanding across {report.summary.active_card_count} active accounts.</p>}<h3>Existing-card import preview</h3><p>Fixture CSV only: source reference, card number, balance. Preview never writes data.</p><textarea value={importText} onChange={e=>setImportText(e.target.value)} placeholder="fixture-001,TESTCARD0001,25.00"/><button onClick={()=>void previewImport()}>DRY RUN IMPORT</button>{result?.rows&&<p>{result.total} rows · {result.ready} ready · {result.duplicates} duplicates · {result.invalid} invalid</p>}{message&&<p role="status">{message}</p>}<style jsx>{`section{padding:20px;border:1px solid #d7d1c4;border-radius:12px;margin-top:18px}section>div{display:flex;flex-wrap:wrap;gap:10px}label,article{display:grid;gap:4px}button{margin:8px 8px 0 0}textarea{width:100%;min-height:80px}`}</style></section>}
